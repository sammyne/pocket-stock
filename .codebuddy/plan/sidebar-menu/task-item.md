# 实施计划

- [ ] 1. 修改 Streamlit 页面配置以启用侧边栏
   - 修改 `main.py` 中的 `set_page_config`，将 `initial_sidebar_state` 从 "collapsed" 改为 "expanded"
   - 确保侧边栏在页面加载时默认展开
   - _需求：1.1、1.2_

- [ ] 2. 重构主页逻辑，将现有功能封装为独立函数
   - 创建 `render_home_page()` 函数，将当前主页的所有股票分析功能代码移动到该函数中
   - 确保函数包含股票代码输入、开始分析按钮、行情/新闻/AI分析标签页等所有现有功能
   - 保持原有功能逻辑不变
   - _需求：4.1、4.2、4.3、4.4_

- [ ] 3. 添加侧边栏导航逻辑和主页菜单项
   - 在侧边栏中添加 `st.page_link` 或 `st.radio` 实现菜单导航
   - 创建"主页"菜单项，关联到主页
   - 在主程序中添加路由逻辑，根据选中的菜单项渲染对应的页面
   - 设置默认选中"主页"菜单项
   - _需求：2.1、2.2、2.3、2.4_

- [ ] 4. 实现设置页面
   - 创建 `render_settings_page()` 函数，显示空白的设置页面内容
   - 在侧边栏中添加"设置"菜单项，关联到设置页面
   - 在路由逻辑中添加设置页面的渲染条件
   - _需求：3.1、3.2、3.3、3.4_

- [ ] 5. 测试侧边栏导航功能
   - 测试主页和设置页面的切换功能
   - 验证菜单项高亮显示是否正确
   - 验证页面刷新后侧边栏状态保持
   - 验证默认选中主页菜单项
   - _需求：1.2、1.3、2.3、2.4、3.3_

- [ ] 6. 测试主页功能完整性
   - 测试股票代码输入功能
   - 测试"开始分析"按钮功能
   - 测试行情、新闻、AI分析三个标签页的显示
   - 确保所有现有功能正常工作
   - _需求：4.1、4.2、4.3、4.4_

- [ ] 7. 测试移动端响应式显示
   - 在小屏幕设备上测试侧边栏的展开/收起功能
   - 验证侧边栏在小屏幕上的可用性
   - _需求：5.1、5.2_
